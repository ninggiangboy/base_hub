server {
    listen 80;

    # Forward /api/admin to localhost:8088
    location /api/admin/ {
        proxy_pass http://host.docker.internal:8088/api/admin/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Forward /api to localhost:8089
    location /api/ {
        proxy_pass http://host.docker.internal:8089/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Optional default page
    location / {
        return 200 'Nginx is running';
    }
}
